version: '3.8'

services:
  dashboard:
    build:
      context: ..
      dockerfile: dashboards/Dockerfile
    container_name: ahgd-dashboard
    ports:
      - "8501:8501"
    volumes:
      # Mount DuckDB database
      - ../ahgd.db:/app/ahgd.db:ro
      # Mount dashboards for development (comment out for production)
      - ./:/app/dashboards
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add nginx reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   container_name: ahgd-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - dashboard
  #   restart: unless-stopped

networks:
  default:
    name: ahgd-network
